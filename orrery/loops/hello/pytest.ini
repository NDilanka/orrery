[pytest]
# Scope collection to THIS seed so the gate measures only mathlib's tests
# (without this, pytest's rootdir discovery can walk up and collect the repo's
# engine tests too).
testpaths = tests
